#include "table.h"
/*
float fitness(){
	switch(operateur){
		case 0 :
			fitness(nextOperateur, and(test1, test2), nextTest);
			break;
		case 1 :
			fitness(nextOperateur, or(test1, test2), nextTest);
			break;
		case 2 :
			fitness(nextOperateur, not(test1), nextTest);
			break;
		default :
			float nbCorrespondances = 0.;
			for(i=0 ; i<16 ; i++){
				if(tableV[i] == truth_table[i])
					nbCorrespondances ++;
			}
			return nbCorrespondances / 16.;
	}
}
*/
